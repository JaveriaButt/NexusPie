<UserControl x:Class="NPIE.Screen.Items.SingleProductScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NPIE.Screen" 
             xmlns:controls="clr-namespace:NPIE.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
            <RowDefinition/> 
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <controls:NPTextBox x:Name="lbl_Title" Text="Product" FontSize="50" FontFamily="{Binding Source={StaticResource AppViewModel},Path=ApplicationDesign.AppFontFamily,UpdateSourceTrigger=PropertyChanged}" Foreground="Black"></controls:NPTextBox>
            
        </Grid>
    </Grid>
</UserControl>
